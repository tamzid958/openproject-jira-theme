node_modules
.next
out
.DS_Store
*.log
.vercel
next-env.d.ts

# Secrets — never commit. Real env files stay local; only the
# .env.local.example template is checked in for onboarding.
.env
.env.*
!.env.local.example
!.env.example

# Local AI assistant scratch space (Claude Code, etc.) — contains
# session-specific notes and memory, not source-of-truth docs.
.claude/
.claude
*.claude/

# Editor / IDE workspace files.
.idea/
.vscode/
*.swp
*.swo
*~

# Build artefacts not covered by .next/.
.turbo/
coverage/

# OS noise.
Thumbs.db
